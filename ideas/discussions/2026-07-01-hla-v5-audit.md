## What was discussed

Audited `HLA-v5` only in `fanat503/Laplace-attention` at commit `12e5a0f99914e89810d169a9adb82baf64ad2bf1` on July 1, 2026.

The user asked for:

- a v5-only repo check
- sterility review
- error/config review
- a saved checkpoint of important metrics

## Decisions made

- Saved a machine-readable checkpoint at [hla-v5-checkpoint-2026-07-01.json](/abs/path/D:/projects/others/laplace/execution/checkpoints/hla-v5-checkpoint-2026-07-01.json).
- Used plain-text artifacts instead of `lavish` because the local `lavish` CLI was not installed.
- Treated the current worktree and the repo's own scripts/tests as authoritative evidence.

## Findings

1. `HLA-v5` is not sterile/runnable as-is because the package layout is broken.
   Evidence:
   - Code exists under `HLA-v5/scr/`, but runtime imports expect `src` in [train_xla.py](/abs/path/D:/projects/others/laplace/HLA-v5/scr/train_xla.py:64) and [make_init.py](/abs/path/D:/projects/others/laplace/HLA-v5/scr/make_init.py:42).
   - Tests also import `src.*`, causing collection failure in [test_data.py](/abs/path/D:/projects/others/laplace/HLA-v5/tests/test_data.py:1), [test_eval.py](/abs/path/D:/projects/others/laplace/HLA-v5/tests/test_eval.py:1), and [test_make_init.py](/abs/path/D:/projects/others/laplace/HLA-v5/tests/test_make_init.py:1).

2. The built-in sterility audit is stale relative to the current tree.
   Evidence:
   - [audit_sterility.py](/abs/path/D:/projects/others/laplace/HLA-v5/scripts/audit_sterility.py:18) requires root files such as `train_xla.py`, `make_init.py`, `src/model.py`, requirement files, `pyproject.toml`, and several docs that do not exist in `HLA-v5`.
   - The latest commit message is `Delete make_init.py` dated June 30, 2026, which is consistent with that drift.

3. The 200M configs are invalid against the model's own config checks.
   Evidence:
   - [200m_base_s42.json](/abs/path/D:/projects/others/laplace/HLA-v5/configs/200m_base_s42.json:1) and [200m_hla_s42.json](/abs/path/D:/projects/others/laplace/HLA-v5/configs/200m_hla_s42.json:1) set `n_head=12` and `n_embd=1024`.
   - [model.py](/abs/path/D:/projects/others/laplace/HLA-v5/scr/model.py:430) raises when `n_embd % n_head != 0`.
   - Duplicate file [configs200m_base_s42.json](/abs/path/D:/projects/others/laplace/HLA-v5/configs/configs200m_base_s42.json:1) repeats the same invalid shape.

4. Config documentation is behind the validator and some pairs are not "sterile matched" under the current rule set.
   Evidence:
   - [configs/README.md](/abs/path/D:/projects/others/laplace/HLA-v5/configs/README.md:8) says the only allowed model diffs include `phase_mult` and `laplace_alpha`.
   - [validate_configs.py](/abs/path/D:/projects/others/laplace/HLA-v5/scripts/validate_configs.py:15) also allows `model.distance_laplace_alpha`.
   - Under that rule set, the 600M, 700M, and 800M base/HLA pairs are clean, but the 200M and 300M pairs also differ on `model.use_laplace`, `model.use_distance_laplace`, and for 300M `max_steps`.

5. Environment validation fails in the current machine context.
   Evidence:
   - `python HLA-v5/scripts/check_environment.py --requirements HLA-v5/requirements_tpu.txt` failed immediately because the active Python is `3.13.5` while the script requires `>=3.10,<3.12`.
   - `HLA-v5` currently has no `requirements_tpu.txt`, `requirements_data.txt`, `requirements_dev.txt`, or `pyproject.toml`.

## Important metrics

- Repo commit: `12e5a0f99914e89810d169a9adb82baf64ad2bf1`
- Last commit date: `2026-06-30 12:05:32 +0300`
- Last commit subject: `Delete make_init.py`
- `HLA-v5` tracked file count: `53`
- Python files: `31`
- Scripts: `20`
- Tests: `3`
- Config JSON files: `19`
- Python syntax compilation: pass via `python -m compileall HLA-v5`
- Pytest status: fail during collection, `3` import errors
- Sterility audit status: fail
- Environment check status: fail
- Main 800M config-pair validation: pass

Main config family metrics:

- `300m_*`: valid shape, approx params `328,198,784`, tokens/update `262,144`, total tokens `6,553,600,000` base and `6,122,110,976` HLA
- `600m_*`: valid shape, approx params `581,859,328`, tokens/update `262,144`, total tokens `15,000,141,824`
- `700m_*`: valid shape, approx params `694,112,384`, tokens/update `262,144`, total tokens `14,000,062,464`
- `800m_*`: valid shape, approx params `816,195,840`, tokens/update `262,144`, total tokens `21,000,093,696`
- `pilot_hla_s42`: valid shape, approx params `816,195,840`, total tokens `262,144,000`
- `smoke_hla_s42`: valid shape, approx params `39,752,256`, tokens/update `2,048`, total tokens `20,480`
- `200m_*`: invalid shape, cannot instantiate under current model checks

## Open questions

- Was `scr/` meant to be renamed back to `src/`, or were import sites left behind during a partial refactor?
- Should the root-level files expected by `audit_sterility.py` be restored, or should the audit be rewritten to match the new layout?
- Are the 200M configs obsolete, or do they need corrected dimensions?
- Is the 300M pair intended to be FLOP-matched rather than strictly sterile-matched?

## Next actions

- Fix the `src`/`scr` mismatch first, then rerun pytest and the sterility audit.
- Decide whether to restore missing root docs/requirements or update `audit_sterility.py` to the current repo structure.
- Remove or correct the invalid 200M configs.
- Reconcile `configs/README.md` with `validate_configs.py` and document which config families are sterile-matched versus compute-matched.

## Important user preferences

- Scope was `v5` only.
- Save a reusable checkpoint of the important metrics locally.
