diff --git a/CONTRIBUTING.md b/CONTRIBUTING.md
index cea757f..d91f520 100644
--- a/CONTRIBUTING.md
+++ b/CONTRIBUTING.md
@@ -2,7 +2,7 @@
 
 ## Ground rules (they exist for scientific validity, not bureaucracy)
 
-1. **No commit without green tests.** `python -m pytest tests/ -q -W error::UserWarning` must print `298 passed` (or more) before any push. CI enforces this, but run locally first.
+1. **No commit without green tests.** `python -m pytest tests/ -q -W error::UserWarning` must print `301 passed` (or more) before any push. CI enforces this, but run locally first.
 2. **Sterility is inviolable.** Any change touching `src/model.py`, `src/make_init.py`, config pairs, or the sampler must keep the identity/parameter-matching/data-order invariants (see `docs/STERILITY.md`). The relevant tests (`test_theory.py::TestTheorem1Identity`, `TestSterility`, `test_make_init.py`) are the gate.
 3. **New mechanisms follow the pattern**: identity-initialized (zero params ⇒ exact no-op, add to `reset_hla_identity` + `hla_identity_error` + `HLA_KEY_PATTERNS` + `HLA_NODECAY_MARKERS`), always-created for parameter matching, alpha-gated, clamped, with saturation diagnostics, an SDPA guard if it biases scores, and tests for identity/causality/gradient-flow/clip.
 4. **Configs change via the validators.** `validate_configs.py` (pair discipline) and `audit_config_values.py` (numeric sanity) must pass; new allowed diffs require justification in the PR description.
diff --git a/configs/200m_base_s42.json b/configs/200m_base_s42.json
index 94d4b91..bd8f431 100644
--- a/configs/200m_base_s42.json
+++ b/configs/200m_base_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 17900,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/200m_base_v2_s42.json b/configs/200m_base_v2_s42.json
index 3e7d845..870e46b 100644
--- a/configs/200m_base_v2_s42.json
+++ b/configs/200m_base_v2_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 17900,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/200m_hla_s42.json b/configs/200m_hla_s42.json
index e8ade80..614338a 100644
--- a/configs/200m_hla_s42.json
+++ b/configs/200m_hla_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 17900,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/200m_hla_v2_flops_s42.json b/configs/200m_hla_v2_flops_s42.json
index 7aa64e7..35c4179 100644
--- a/configs/200m_hla_v2_flops_s42.json
+++ b/configs/200m_hla_v2_flops_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 16740,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/200m_hla_v2_s42.json b/configs/200m_hla_v2_s42.json
index ccc4aa8..300fbc8 100644
--- a/configs/200m_hla_v2_s42.json
+++ b/configs/200m_hla_v2_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 17900,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_abl_base_template.json b/configs/kaggle_abl_base_template.json
index 018c3c9..95b205a 100644
--- a/configs/kaggle_abl_base_template.json
+++ b/configs/kaggle_abl_base_template.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 7000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_abl_hla_template.json b/configs/kaggle_abl_hla_template.json
index 6ab0acf..6e2a516 100644
--- a/configs/kaggle_abl_hla_template.json
+++ b/configs/kaggle_abl_hla_template.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 7000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_pilot_base_s42.json b/configs/kaggle_pilot_base_s42.json
index e00e1a7..05fbe24 100644
--- a/configs/kaggle_pilot_base_s42.json
+++ b/configs/kaggle_pilot_base_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 1000,
@@ -28,7 +28,7 @@
   "log_every": 20,
   "val_every": 100,
   "svd_every": 500,
-  "val_batches": 16,
+  "val_batches": 64,
   "resume_every": 200,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_pilot_hla_s42.json b/configs/kaggle_pilot_hla_s42.json
index 16552d9..3edb033 100644
--- a/configs/kaggle_pilot_hla_s42.json
+++ b/configs/kaggle_pilot_hla_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 1000,
@@ -28,7 +28,7 @@
   "log_every": 20,
   "val_every": 100,
   "svd_every": 500,
-  "val_batches": 16,
+  "val_batches": 64,
   "resume_every": 200,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_smoke_base_s42.json b/configs/kaggle_smoke_base_s42.json
index 45d8731..70cd6de 100644
--- a/configs/kaggle_smoke_base_s42.json
+++ b/configs/kaggle_smoke_base_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 10,
@@ -28,7 +28,7 @@
   "log_every": 1,
   "val_every": 5,
   "svd_every": 5,
-  "val_batches": 4,
+  "val_batches": 16,
   "resume_every": 100,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_smoke_hla_s42.json b/configs/kaggle_smoke_hla_s42.json
index 28e92a9..92e490a 100644
--- a/configs/kaggle_smoke_hla_s42.json
+++ b/configs/kaggle_smoke_hla_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 10,
@@ -28,7 +28,7 @@
   "log_every": 1,
   "val_every": 5,
   "svd_every": 5,
-  "val_batches": 4,
+  "val_batches": 16,
   "resume_every": 100,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_s42.json b/configs/tpu3_200m_base_s42.json
index ed934cc..f956ac9 100644
--- a/configs/tpu3_200m_base_s42.json
+++ b/configs/tpu3_200m_base_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_v2_flopslong_s42.json b/configs/tpu3_200m_base_v2_flopslong_s42.json
index 116fe7c..26eaec6 100644
--- a/configs/tpu3_200m_base_v2_flopslong_s42.json
+++ b/configs/tpu3_200m_base_v2_flopslong_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 21386,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_v2_s42.json b/configs/tpu3_200m_base_v2_s42.json
index 2a577c0..799d5ce 100644
--- a/configs/tpu3_200m_base_v2_s42.json
+++ b/configs/tpu3_200m_base_v2_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_v2_s43.json b/configs/tpu3_200m_base_v2_s43.json
index 76ceb13..0220df9 100644
--- a/configs/tpu3_200m_base_v2_s43.json
+++ b/configs/tpu3_200m_base_v2_s43.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_v2_s44.json b/configs/tpu3_200m_base_v2_s44.json
index baccc6e..ca9cc34 100644
--- a/configs/tpu3_200m_base_v2_s44.json
+++ b/configs/tpu3_200m_base_v2_s44.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_hla_s42.json b/configs/tpu3_200m_hla_s42.json
index e2b147a..9fe1ff9 100644
--- a/configs/tpu3_200m_hla_s42.json
+++ b/configs/tpu3_200m_hla_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_hla_v2_s42.json b/configs/tpu3_200m_hla_v2_s42.json
index 795a842..6632fd7 100644
--- a/configs/tpu3_200m_hla_v2_s42.json
+++ b/configs/tpu3_200m_hla_v2_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_hla_v2_s43.json b/configs/tpu3_200m_hla_v2_s43.json
index 719babf..cd71940 100644
--- a/configs/tpu3_200m_hla_v2_s43.json
+++ b/configs/tpu3_200m_hla_v2_s43.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_hla_v2_s44.json b/configs/tpu3_200m_hla_v2_s44.json
index 7ba671c..a9f276a 100644
--- a/configs/tpu3_200m_hla_v2_s44.json
+++ b/configs/tpu3_200m_hla_v2_s44.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 16,
-  "eval_batch_size_per_device": 16,
-  "grad_accum": 1,
+  "batch_size_per_device": 2,
+  "eval_batch_size_per_device": 4,
+  "grad_accum": 8,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 50,
+  "val_batches": 200,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/tests/test_train_utils.py b/tests/test_train_utils.py
index 1a0c3e5..c58fb0f 100644
--- a/tests/test_train_utils.py
+++ b/tests/test_train_utils.py
@@ -410,3 +410,78 @@ class TestDryRunHarness:
         src = open(os.path.join(ROOT, "scripts", "dry_run_cpu.py")).read()
         assert "torch_xla.runtime" in src
         assert "global_ordinal" in src and "world_size" in src
+
+
+class TestHbmBudget:
+    """Day-1 finding #7 (the REAL cause of the first OOM, discovered after
+    the world-size fix): fp32 softmax materializes [B*H, T, T] per layer and
+    the backward pass keeps ALL layers' buffers alive. At b=16, T=2048, H=16:
+    4 GB x 12 layers = 48 GB >> 15.75 GB HBM - the config could never fit,
+    on any runtime. Measured: 'Used 68.25G of 15.75G hbm', twelve
+    f32[256,2048,2048] allocations."""
+
+    HBM_BYTES = 15.75 * 2**30
+    # Leave headroom for params+optimizer (~2.5 GB at 200m) and XLA temps.
+    ATTN_BUDGET = 10.0 * 2**30
+
+    @staticmethod
+    def _attn_bytes(cfg):
+        m = cfg["model"]
+        b = int(cfg["batch_size_per_device"])
+        # fp32 softmax buffer per layer, all layers live during backward.
+        return int(m["n_layer"]) * b * int(m["n_head"]) * int(m["block_size"]) ** 2 * 4
+
+    def test_tpu_v3_configs_fit_hbm(self):
+        import glob as _glob, json as _json
+        pats = ("kaggle_*.json", "200m_*.json", "tpu3_200m_*.json")
+        checked = 0
+        for pat in pats:
+            for p in sorted(_glob.glob(os.path.join(ROOT, "configs", pat))):
+                cfg = _json.load(open(p))
+                got = self._attn_bytes(cfg)
+                assert got <= self.ATTN_BUDGET, (
+                    f"{os.path.basename(p)}: fp32-softmax residency "
+                    f"{got/2**30:.1f} GB > {self.ATTN_BUDGET/2**30:.1f} GB budget "
+                    f"(HBM 15.75). Lower batch_size_per_device, raise grad_accum.")
+                checked += 1
+        assert checked >= 20
+
+    def test_tokens_per_update_invariant_preserved(self):
+        """The OOM fix (b 16->2, accum 1->8) must NOT change the optimization
+        trajectory: tokens/update stays 262,144 for every 8-core 200m config."""
+        import glob as _glob, json as _json
+        pats = ("kaggle_*.json", "200m_*.json", "tpu3_200m_*.json")
+        for pat in pats:
+            for p in sorted(_glob.glob(os.path.join(ROOT, "configs", pat))):
+                cfg = _json.load(open(p))
+                tpu = (int(cfg["batch_size_per_device"]) * 8
+                       * int(cfg["model"]["block_size"]) * int(cfg["grad_accum"]))
+                assert tpu == 262144, f"{os.path.basename(p)}: tokens/update {tpu}"
+
+    def test_grad_accum_preserves_update_math(self):
+        """The HBM fix changes b16/accum1 -> b2/accum8. This must be the SAME
+        optimization step: sum of (loss/accum).backward() over 8 microbatches
+        equals one full-batch backward (fp32, linearity of gradients).
+        Measured on this repo's GPT: max weight diff 3.7e-09 after 1 update."""
+        import torch as _t
+        from src.model import GPT, GPTConfig
+        cfg = GPTConfig(block_size=32, vocab_size=256, n_layer=1, n_head=2,
+                        n_embd=32, gradient_checkpointing=False)
+
+        def one_update(accum):
+            _t.manual_seed(0)
+            m = GPT(cfg)
+            opt = _t.optim.SGD(m.parameters(), lr=0.1)
+            _t.manual_seed(42)
+            X = _t.randint(0, 256, (16, 32))
+            Y = _t.randint(0, 256, (16, 32))
+            opt.zero_grad()
+            mb = 16 // accum
+            for i in range(accum):
+                _, loss = m(X[i * mb:(i + 1) * mb], Y[i * mb:(i + 1) * mb])
+                (loss / accum).backward()
+            opt.step()
+            return _t.cat([p.detach().flatten() for p in m.parameters()])
+
+        diff = (one_update(1) - one_update(8)).abs().max().item()
+        assert diff < 1e-5, f"accum changed the update: max weight diff {diff}"
