diff --git a/CONTRIBUTING.md b/CONTRIBUTING.md
index 112b274..c988694 100644
--- a/CONTRIBUTING.md
+++ b/CONTRIBUTING.md
@@ -2,7 +2,7 @@
 
 ## Ground rules (they exist for scientific validity, not bureaucracy)
 
-1. **No commit without green tests.** `python -m pytest tests/ -q -W error::UserWarning` must print `316 passed` (or more) before any push. CI enforces this, but run locally first.
+1. **No commit without green tests.** `python -m pytest tests/ -q -W error::UserWarning` must print `318 passed` (or more) before any push. CI enforces this, but run locally first.
 2. **Sterility is inviolable.** Any change touching `src/model.py`, `src/make_init.py`, config pairs, or the sampler must keep the identity/parameter-matching/data-order invariants (see `docs/STERILITY.md`). The relevant tests (`test_theory.py::TestTheorem1Identity`, `TestSterility`, `test_make_init.py`) are the gate.
 3. **New mechanisms follow the pattern**: identity-initialized (zero params ⇒ exact no-op, add to `reset_hla_identity` + `hla_identity_error` + `HLA_KEY_PATTERNS` + `HLA_NODECAY_MARKERS`), always-created for parameter matching, alpha-gated, clamped, with saturation diagnostics, an SDPA guard if it biases scores, and tests for identity/causality/gradient-flow/clip.
 4. **Configs change via the validators.** `validate_configs.py` (pair discipline) and `audit_config_values.py` (numeric sanity) must pass; new allowed diffs require justification in the PR description.
diff --git a/configs/200m_base_s42.json b/configs/200m_base_s42.json
index bd8f431..d8ed275 100644
--- a/configs/200m_base_s42.json
+++ b/configs/200m_base_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 17900,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/200m_base_v2_s42.json b/configs/200m_base_v2_s42.json
index 870e46b..c905f1f 100644
--- a/configs/200m_base_v2_s42.json
+++ b/configs/200m_base_v2_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 17900,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/200m_hla_s42.json b/configs/200m_hla_s42.json
index 614338a..370be59 100644
--- a/configs/200m_hla_s42.json
+++ b/configs/200m_hla_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 17900,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/200m_hla_v2_flops_s42.json b/configs/200m_hla_v2_flops_s42.json
index 35c4179..32c5401 100644
--- a/configs/200m_hla_v2_flops_s42.json
+++ b/configs/200m_hla_v2_flops_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 16740,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/200m_hla_v2_s42.json b/configs/200m_hla_v2_s42.json
index 300fbc8..bb9fb50 100644
--- a/configs/200m_hla_v2_s42.json
+++ b/configs/200m_hla_v2_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 17900,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_200m_base_9h_s42.json b/configs/kaggle_200m_base_9h_s42.json
new file mode 100644
index 0000000..6902970
--- /dev/null
+++ b/configs/kaggle_200m_base_9h_s42.json
@@ -0,0 +1,75 @@
+{
+  "_doc": "200m single-session-first run: 15000 steps = 3.93B tokens = 18.1 tok/param. At V4-measured 0.53 steps/sec (b=2) total is ~8.3h -> one 9h batch session. If b=1 turns out slower, resume_every=500 + auto-resume in the launch cell finishes the tail in a short follow-up session (<=500 steps lost). Full 4.7B (17900) stays in tpu3_* for the month grant.",
+  "seed": 42,
+  "save_dir": "/kaggle/working/runs/kaggle_200m_base_9h_s42",
+  "run_name": "kaggle_200m_base_9h_s42",
+  "train_path": "/kaggle/input/hla-c4-tokens/train_fixed_tokens.bin",
+  "val_path": "/kaggle/input/hla-c4-tokens/val_fixed_tokens.bin",
+  "init_ckpt": "/kaggle/working/inits/init_200m_s42_base.pt",
+  "init_strict": true,
+  "check_init_hla_identity": true,
+  "expected_vocab_size": 50257,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
+  "num_workers": 0,
+  "dataloader_prefetch_factor": 4,
+  "max_steps": 15000,
+  "warmup": 750,
+  "grad_clip": 1.0,
+  "lr": 0.0006,
+  "min_lr": 6e-05,
+  "weight_decay": 0.1,
+  "betas": [
+    0.9,
+    0.95
+  ],
+  "adam_eps": 1e-08,
+  "log_every": 50,
+  "val_every": 500,
+  "svd_every": 2500,
+  "val_batches": 100,
+  "resume_every": 500,
+  "save_every": 0,
+  "keep_last_checkpoints": 2,
+  "min_free_gb": 5.0,
+  "num_cores": 8,
+  "xla_loader_prefetch_size": 8,
+  "xla_device_prefetch_size": 4,
+  "validate_dataset_full": false,
+  "fail_on_nonfinite_val": true,
+  "allow_multiple_epochs": false,
+  "model": {
+    "block_size": 2048,
+    "vocab_size": 50257,
+    "n_layer": 12,
+    "n_head": 16,
+    "n_embd": 1024,
+    "dropout": 0.0,
+    "bias": false,
+    "gradient_checkpointing": false,
+    "fused_swiglu": true,
+    "use_rope": true,
+    "phase_mult": 0.0,
+    "use_laplace": true,
+    "laplace_alpha": 0.0,
+    "laplace_range_k": 0.8,
+    "laplace_range_v": 0.5,
+    "beta_k": 0.7,
+    "beta_v": 0.5,
+    "k_log_clip": 2.0,
+    "v_log_clip": 1.5,
+    "layer_dependent_gate": false,
+    "use_distance_laplace": true,
+    "distance_laplace_alpha": 0.0,
+    "distance_laplace_range": 1.0,
+    "distance_laplace_clip": 1.0,
+    "use_wpe": false,
+    "use_salience_bias": true,
+    "salience_alpha": 0.0,
+    "salience_range": 2.0,
+    "salience_clip": 3.0,
+    "baseline_type": "base_pm_ablated_v2"
+  },
+  "variant": "base_pm_ablated_v2"
+}
diff --git a/configs/kaggle_200m_hla_9h_s42.json b/configs/kaggle_200m_hla_9h_s42.json
new file mode 100644
index 0000000..d3dddd2
--- /dev/null
+++ b/configs/kaggle_200m_hla_9h_s42.json
@@ -0,0 +1,75 @@
+{
+  "_doc": "200m single-session-first run: 15000 steps = 3.93B tokens = 18.1 tok/param. At V4-measured 0.53 steps/sec (b=2) total is ~8.3h -> one 9h batch session. If b=1 turns out slower, resume_every=500 + auto-resume in the launch cell finishes the tail in a short follow-up session (<=500 steps lost). Full 4.7B (17900) stays in tpu3_* for the month grant.",
+  "seed": 42,
+  "save_dir": "/kaggle/working/runs/kaggle_200m_hla_9h_s42",
+  "run_name": "kaggle_200m_hla_9h_s42",
+  "train_path": "/kaggle/input/hla-c4-tokens/train_fixed_tokens.bin",
+  "val_path": "/kaggle/input/hla-c4-tokens/val_fixed_tokens.bin",
+  "init_ckpt": "/kaggle/working/inits/init_200m_s42_hla.pt",
+  "init_strict": true,
+  "check_init_hla_identity": true,
+  "expected_vocab_size": 50257,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
+  "num_workers": 0,
+  "dataloader_prefetch_factor": 4,
+  "max_steps": 15000,
+  "warmup": 750,
+  "grad_clip": 1.0,
+  "lr": 0.0006,
+  "min_lr": 6e-05,
+  "weight_decay": 0.1,
+  "betas": [
+    0.9,
+    0.95
+  ],
+  "adam_eps": 1e-08,
+  "log_every": 50,
+  "val_every": 500,
+  "svd_every": 2500,
+  "val_batches": 100,
+  "resume_every": 500,
+  "save_every": 0,
+  "keep_last_checkpoints": 2,
+  "min_free_gb": 5.0,
+  "num_cores": 8,
+  "xla_loader_prefetch_size": 8,
+  "xla_device_prefetch_size": 4,
+  "validate_dataset_full": false,
+  "fail_on_nonfinite_val": true,
+  "allow_multiple_epochs": false,
+  "model": {
+    "block_size": 2048,
+    "vocab_size": 50257,
+    "n_layer": 12,
+    "n_head": 16,
+    "n_embd": 1024,
+    "dropout": 0.0,
+    "bias": false,
+    "gradient_checkpointing": false,
+    "fused_swiglu": true,
+    "use_rope": true,
+    "phase_mult": 0.15,
+    "use_laplace": true,
+    "laplace_alpha": 1.0,
+    "laplace_range_k": 0.8,
+    "laplace_range_v": 0.5,
+    "beta_k": 0.7,
+    "beta_v": 0.5,
+    "k_log_clip": 2.0,
+    "v_log_clip": 1.5,
+    "layer_dependent_gate": false,
+    "use_distance_laplace": true,
+    "distance_laplace_alpha": 0.5,
+    "distance_laplace_range": 1.0,
+    "distance_laplace_clip": 1.0,
+    "use_wpe": false,
+    "use_salience_bias": true,
+    "salience_alpha": 1.0,
+    "salience_range": 2.0,
+    "salience_clip": 3.0,
+    "baseline_type": "hla_v2_aggressive_gates_salience"
+  },
+  "variant": "hla_v2_aggressive_gates_salience"
+}
diff --git a/configs/kaggle_abl_base_template.json b/configs/kaggle_abl_base_template.json
index 95b205a..075a905 100644
--- a/configs/kaggle_abl_base_template.json
+++ b/configs/kaggle_abl_base_template.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 7000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_abl_hla_template.json b/configs/kaggle_abl_hla_template.json
index 6e2a516..42c099e 100644
--- a/configs/kaggle_abl_hla_template.json
+++ b/configs/kaggle_abl_hla_template.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 7000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_pilot_base_s42.json b/configs/kaggle_pilot_base_s42.json
index 05fbe24..a72033b 100644
--- a/configs/kaggle_pilot_base_s42.json
+++ b/configs/kaggle_pilot_base_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 1000,
@@ -28,7 +28,7 @@
   "log_every": 20,
   "val_every": 100,
   "svd_every": 500,
-  "val_batches": 64,
+  "val_batches": 128,
   "resume_every": 200,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_pilot_hla_s42.json b/configs/kaggle_pilot_hla_s42.json
index 3edb033..7ac1c81 100644
--- a/configs/kaggle_pilot_hla_s42.json
+++ b/configs/kaggle_pilot_hla_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 1000,
@@ -28,7 +28,7 @@
   "log_every": 20,
   "val_every": 100,
   "svd_every": 500,
-  "val_batches": 64,
+  "val_batches": 128,
   "resume_every": 200,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_smoke_base_s42.json b/configs/kaggle_smoke_base_s42.json
index 70cd6de..032b98b 100644
--- a/configs/kaggle_smoke_base_s42.json
+++ b/configs/kaggle_smoke_base_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 10,
@@ -28,7 +28,7 @@
   "log_every": 1,
   "val_every": 5,
   "svd_every": 5,
-  "val_batches": 16,
+  "val_batches": 32,
   "resume_every": 100,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/kaggle_smoke_hla_s42.json b/configs/kaggle_smoke_hla_s42.json
index 92e490a..0f8b9f9 100644
--- a/configs/kaggle_smoke_hla_s42.json
+++ b/configs/kaggle_smoke_hla_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 10,
@@ -28,7 +28,7 @@
   "log_every": 1,
   "val_every": 5,
   "svd_every": 5,
-  "val_batches": 16,
+  "val_batches": 32,
   "resume_every": 100,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_s42.json b/configs/tpu3_200m_base_s42.json
index f956ac9..df8c992 100644
--- a/configs/tpu3_200m_base_s42.json
+++ b/configs/tpu3_200m_base_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_v2_flopslong_s42.json b/configs/tpu3_200m_base_v2_flopslong_s42.json
index 26eaec6..18bdecd 100644
--- a/configs/tpu3_200m_base_v2_flopslong_s42.json
+++ b/configs/tpu3_200m_base_v2_flopslong_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 21386,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_v2_s42.json b/configs/tpu3_200m_base_v2_s42.json
index 799d5ce..4d71a91 100644
--- a/configs/tpu3_200m_base_v2_s42.json
+++ b/configs/tpu3_200m_base_v2_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_v2_s43.json b/configs/tpu3_200m_base_v2_s43.json
index 0220df9..fbeafad 100644
--- a/configs/tpu3_200m_base_v2_s43.json
+++ b/configs/tpu3_200m_base_v2_s43.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_base_v2_s44.json b/configs/tpu3_200m_base_v2_s44.json
index ca9cc34..a290124 100644
--- a/configs/tpu3_200m_base_v2_s44.json
+++ b/configs/tpu3_200m_base_v2_s44.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_hla_s42.json b/configs/tpu3_200m_hla_s42.json
index 9fe1ff9..fccad32 100644
--- a/configs/tpu3_200m_hla_s42.json
+++ b/configs/tpu3_200m_hla_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_hla_v2_s42.json b/configs/tpu3_200m_hla_v2_s42.json
index 6632fd7..81097b6 100644
--- a/configs/tpu3_200m_hla_v2_s42.json
+++ b/configs/tpu3_200m_hla_v2_s42.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_hla_v2_s43.json b/configs/tpu3_200m_hla_v2_s43.json
index cd71940..1b57cc6 100644
--- a/configs/tpu3_200m_hla_v2_s43.json
+++ b/configs/tpu3_200m_hla_v2_s43.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/configs/tpu3_200m_hla_v2_s44.json b/configs/tpu3_200m_hla_v2_s44.json
index a9f276a..246f2d4 100644
--- a/configs/tpu3_200m_hla_v2_s44.json
+++ b/configs/tpu3_200m_hla_v2_s44.json
@@ -9,9 +9,9 @@
   "init_strict": true,
   "check_init_hla_identity": true,
   "expected_vocab_size": 50257,
-  "batch_size_per_device": 2,
-  "eval_batch_size_per_device": 4,
-  "grad_accum": 8,
+  "batch_size_per_device": 1,
+  "eval_batch_size_per_device": 2,
+  "grad_accum": 16,
   "num_workers": 0,
   "dataloader_prefetch_factor": 4,
   "max_steps": 20000,
@@ -28,7 +28,7 @@
   "log_every": 100,
   "val_every": 500,
   "svd_every": 2000,
-  "val_batches": 200,
+  "val_batches": 400,
   "resume_every": 500,
   "save_every": 0,
   "keep_last_checkpoints": 2,
diff --git a/scripts/make_paper_figures.py b/scripts/make_paper_figures.py
index c766a6a..b438706 100644
--- a/scripts/make_paper_figures.py
+++ b/scripts/make_paper_figures.py
@@ -150,11 +150,81 @@ def fig3_gap_closure(causal_json, out):
     print(f"wrote {out}")
 
 
+def fig4_knockout_context(analysis_json, out):
+    """Mechanism knockout Dloss vs context length (H2: the long-range
+    mechanisms must MATTER MORE as context grows - the quantitative
+    long-context evidence, one line per mechanism)."""
+    plt = require_matplotlib()
+    res = json.load(open(analysis_json, encoding="utf-8"))
+    ko = res.get("knockout_by_context_length")
+    if not ko:
+        raise SystemExit("analysis JSON lacks knockout_by_context_length")
+    lengths = sorted(int(k) for k in ko)
+    mechs = sorted({m for L in ko.values() for m in L if m.startswith("ko_")})
+    fig, ax = plt.subplots(figsize=(4.6, 3.2))
+    for m in mechs:
+        ys = [ko[str(L)].get(m, float("nan")) for L in lengths]
+        ax.plot(lengths, ys, "o-", ms=3.5, lw=1.3,
+                label=m.replace("ko_", "").replace("_delta", ""))
+    ax.axhline(0, color="k", lw=0.6)
+    ax.set_xlabel("context length (tokens)")
+    ax.set_ylabel("knockout \u0394loss")
+    ax.set_xscale("log", base=2)
+    ax.legend(fontsize=7, ncol=2)
+    fig.savefig(out)
+    plt.close(fig)
+    print(f"wrote {out}")
+
+
+def fig5_mechanism_trajectories(base_log, hla_log, out):
+    """Internal dynamics over training (GDN/Diff reviewer lesson: show WHEN
+    mechanisms wake up, not just that they exist): gate/salience activity,
+    saturation, and retrieval probes on one page, base vs HLA."""
+    plt = require_matplotlib()
+    panels = [
+        ("Gate activity", ["gate_k_mean", "gate_v_mean"]),
+        ("Mix envelope", ["mix_k_mean", "mix_v_mean"]),
+        ("Saturation", ["gate_k_sat_frac", "angle_q_sat_frac"]),
+        ("Retrieval probes", ["induction", "distractor_margin"]),
+        ("LITM scalars", ["litm_middle_drop", "litm_worst_frac"]),
+        ("Head interference", ["qk_interference", "qk_ov_separation"]),
+    ]
+    logs = {"base": read_log(base_log), "HLA": read_log(hla_log)}
+    colors = {"base": BASE_COLOR, "HLA": HLA_COLOR}
+    fig, axes = plt.subplots(2, 3, figsize=(11, 5.6))
+    for ax, (title, cols) in zip(axes.flat, panels):
+        drew = False
+        for name, log in logs.items():
+            for i, col in enumerate(cols):
+                if col not in log:
+                    continue
+                pairs = [(x, y) for x, y in zip(log["tokens_seen"], log[col])
+                         if not math.isnan(y)]
+                if not pairs:
+                    continue
+                ax.plot([p[0] for p in pairs], [p[1] for p in pairs],
+                        ["-", "--"][i % 2], color=colors[name], lw=1.2,
+                        label=f"{name}:{col}", alpha=0.9)
+                drew = True
+        ax.set_title(title, fontsize=9)
+        ax.set_xlabel("tokens", fontsize=8)
+        if drew:
+            ax.legend(fontsize=5.5)
+        else:
+            ax.text(0.5, 0.5, "no data", ha="center", va="center",
+                    transform=ax.transAxes, color="gray")
+    fig.tight_layout()
+    fig.savefig(out)
+    plt.close(fig)
+    print(f"wrote {out}")
+
+
 def main() -> None:
     ap = argparse.ArgumentParser(description="Paper-grade figures from run artifacts")
     ap.add_argument("--base-log")
     ap.add_argument("--hla-log")
     ap.add_argument("--causal-json")
+    ap.add_argument("--analysis-json")
     ap.add_argument("--seed-std", type=float, default=0.0,
                     help="val-loss std across seeds (for the gap inset)")
     ap.add_argument("--out-dir", required=True)
@@ -175,6 +245,19 @@ def main() -> None:
                              os.path.join(args.out_dir, "fig3_gap_closure.png"))
         except SystemExit as e:
             print(f"[skip fig3] {e}")
+    if args.base_log and args.hla_log:
+        try:
+            fig5_mechanism_trajectories(
+                args.base_log, args.hla_log,
+                os.path.join(args.out_dir, "fig5_mechanism_trajectories.png"))
+        except SystemExit as e:
+            print(f"[skip fig5] {e}")
+    if args.analysis_json:
+        try:
+            fig4_knockout_context(args.analysis_json,
+                                  os.path.join(args.out_dir, "fig4_knockout_context.png"))
+        except (SystemExit, OSError) as e:
+            print(f"[skip fig4] {e}")
 
 
 if __name__ == "__main__":
diff --git a/tests/test_ablation_configs.py b/tests/test_ablation_configs.py
index 5be0509..b5a74d4 100644
--- a/tests/test_ablation_configs.py
+++ b/tests/test_ablation_configs.py
@@ -875,3 +875,16 @@ class TestPaperFigures:
                         if uses <= 1 and name not in ("annotations",):
                             offenders.append(f"{os.path.basename(path)}:{name}")
         assert not offenders, f"unused imports: {offenders}"
+
+    def test_paper_figures_has_five_figures(self):
+        """fig4 (knockout vs context: H2 long-range evidence) and fig5
+        (mechanism trajectories: GDN/Diff 'inner dynamics' reviewer ask)
+        joined fig1-3; the figure pipeline must expose all five."""
+        import ast
+        path = os.path.join(ROOT, "scripts", "make_paper_figures.py")
+        tree = ast.parse(open(path).read())
+        fns = {n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}
+        for fn in ("fig1_twin_divergence", "fig2_litm_curves",
+                   "fig3_gap_closure", "fig4_knockout_context",
+                   "fig5_mechanism_trajectories"):
+            assert fn in fns, f"missing {fn}"
diff --git a/tests/test_train_utils.py b/tests/test_train_utils.py
index 66adbd6..fab7c7d 100644
--- a/tests/test_train_utils.py
+++ b/tests/test_train_utils.py
@@ -428,8 +428,18 @@ class TestHbmBudget:
     def _attn_bytes(cfg):
         m = cfg["model"]
         b = int(cfg["batch_size_per_device"])
-        # fp32 softmax buffer per layer, all layers live during backward.
-        return int(m["n_layer"]) * b * int(m["n_head"]) * int(m["block_size"]) ** 2 * 4
+        # Finding #10 (v5e smoke): the fp32 softmax buffer is NOT alone.
+        # Every active score-bias mechanism (distance / salience / forget)
+        # materializes its own [B, H, T, T] tensor that backward keeps
+        # alive per layer. The base twin passed at b=2 while HLA hit
+        # 'Attempting to reserve 9.37G ... 9.28G free' - because this
+        # counter ignored the bias tensors. Count them.
+        n_buffers = 1
+        n_buffers += 1 if m.get("use_distance_laplace") else 0
+        n_buffers += 1 if m.get("use_salience_bias") else 0
+        n_buffers += 1 if m.get("use_forget_gate") or m.get("forget_alpha") else 0
+        return (int(m["n_layer"]) * b * int(m["n_head"])
+                * int(m["block_size"]) ** 2 * 4 * n_buffers)
 
     def test_tpu_v3_configs_fit_hbm(self):
         import glob as _glob, json as _json
@@ -447,8 +457,8 @@ class TestHbmBudget:
         assert checked >= 20
 
     def test_tokens_per_update_invariant_preserved(self):
-        """The OOM fix (b 16->2, accum 1->8) must NOT change the optimization
-        trajectory: tokens/update stays 262,144 for every 8-core 200m config."""
+        """The OOM fixes (b16->2->1, accum 1->8->16) must NOT change the
+        optimization trajectory: tokens/update stays 262,144 everywhere."""
         import glob as _glob, json as _json
         pats = ("kaggle_*.json", "200m_*.json", "tpu3_200m_*.json")
         for pat in pats:
@@ -527,3 +537,31 @@ class TestHbmBudget:
                              os.path.join(ROOT, "scripts", "validate_log.py"),
                              str(p2)], capture_output=True, text=True)
         assert r2.returncode != 0
+
+    def test_9h_configs_fit_one_session(self):
+        """Session policy (user + measured V4 numbers): a run must fit one
+        9h batch session AT THE MEASURED SPEED (0.53 steps/sec, V4), and if
+        hardware turns out slower the auto-resume tail must be bounded
+        (resume_every <= 500). 15000 steps = 8.3h at 0.53 -> single session;
+        worst case -20% -> ~1.8h follow-up, <=500 steps lost. This trades
+        the old hard-pessimism bound for +0.78B tokens (14.5 -> 18.1
+        tok/param), which strengthens the primary comparison."""
+        import glob as _glob, json as _json
+        found = 0
+        for p in sorted(_glob.glob(os.path.join(ROOT, "configs", "kaggle_200m_*_9h_*.json"))):
+            c = _json.load(open(p))
+            steps = int(c["max_steps"])
+            sps = 0.53  # V4-measured steady-state
+            total_h = (steps / sps
+                       + steps / int(c["val_every"]) * 25
+                       + steps / int(c["svd_every"]) * 110) / 3600
+            assert total_h <= 8.5, (
+                f"{os.path.basename(p)}: {total_h:.1f}h > 8.5h at measured "
+                "speed - does not fit a 9h session even optimistically")
+            # twin-pair invariants
+            assert (c["batch_size_per_device"] * 8 * c["model"]["block_size"]
+                    * c["grad_accum"]) == 262144
+            assert steps * 262144 <= 4_700_000_000
+            assert int(c["resume_every"]) <= 500, "need resume for session-splits"
+            found += 1
+        assert found == 2, "expected the base+hla 9h pair"
