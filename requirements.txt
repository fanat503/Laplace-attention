# Core (tests, init generation, analysis - CPU is enough)
# Pin ranges chosen for reproducibility; exact versions recorded per-run in
# environment.json by the trainer.
torch>=2.4,<2.15
numpy>=1.26,<3.0

# Dev / CI
pytest>=8.0
pyflakes>=3.2

# TPU training (Kaggle v5e-8 / TRC). Install the torch_xla matching your torch:
#   pip install torch~=2.5.0 torch_xla[tpu]~=2.5.0 \
#     -f https://storage.googleapis.com/libtpu-releases/index.html
# torch_xla  # (uncomment on TPU hosts only)

# Data preparation (only for prepare_c4_data.py / prepare_data.py):
#   pip install "hla[data]"  ...or manually:
# datasets>=2.19
# tiktoken>=0.6
# tqdm>=4.66
# gigatoken>=0.9   # OPTIONAL fast tokenizer backend (--tokenizer-backend gigatoken);
#                  # verified bit-identical to tiktoken up front + per-batch spot-check
